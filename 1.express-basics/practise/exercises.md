# Practice Exercises

1. Change the home message in `server.js`.
2. Add a `/greet` route that says hello.

## Optional check

Open `http://localhost:3000` after `npm start` and confirm you see the welcome message.

## Challenge

Try adding a `/about` route that returns your name.

